package com.therxmv.project8.utils

object Session {
    const val AUTH_USERNAME = "oauth-client"
    const val AUTH_PASSWORD = "oauth-secret"
    const val BASE_URL = "http://localhost:9090"
}

object CircuitBreaker {
    const val TIME_SERVICE = "TimeService"
}